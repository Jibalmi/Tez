# Tez's results, one chart per finding

Generated by `python experiments/make_dashboard.py` and `python experiments/make_lab_figure.py`, which also rewrite
this file; do not edit it by hand. Each chart states its finding as its title, the conditions under it and its source
in a small footer; every number is read from the files named there. The same panels, with short titles, make up the
two grids: [`../tez_dashboard.png`](../tez_dashboard.png) (every measurement) and [`../tez_lab.png`](../tez_lab.png)
(the probe lab). Laya's own comparison charts, redrawn with Tez, are in [`../vs/`](../vs/README.md).

**Colours** follow `docs/figures/vs/`: Tez blue (hatched = a probe trained on labelled rows; tinted = as shipped or
before a fix; outlined = Tez's letters at temperature 1, before its default temperature), laya magenta,
laya-multilingual amber, laya-typed-decisions green, Jev (published, never run here) grey. A series that is not a
system (a policy, a readout, a model size) is drawn in Tez's blues or in neutral grey, the one the finding is about in
Tez blue.

## Results

### Accuracy against Laya and Jev

| Chart | Finding (its title) | Source |
|---|---|---|
| [`public_tasks.png`](public_tasks.png) | Zero-shot Tez leads every Laya checkpoint on 5 of 9 public tasks on identical rows; Laya leads the other 4, and Jev's published figures lead 3 of the 4 they cover | BENCHMARKS.md §1 (experiments/bench_h2h.py, results/h2h/summary.json); Jev: third-party published, as collected there |
| [`typed_decisions.png`](typed_decisions.png) | On typed-decisions zero-shot Tez scores 0.704 (0.725 with 4 worked examples), against Jev's published 0.727 and 0.766 for laya-typed-decisions, fine-tuned on the benchmark; laya's base checkpoints score 0.362 and 0.352 | BENCHMARKS.md §1 (experiments/bench_h2h.py, results/h2h/summary.json); 4 examples: results/h2h/rows_typed_decisions_tez-fewshot4-all.summary.json (§4b); teacher ceiling, majority class and random: Laya's README (published) |
| [`typed_decisions_by_workflow.png`](typed_decisions_by_workflow.png) | By workflow, laya-typed-decisions (fine-tuned on this benchmark) leads zero-shot Tez on 3 of 4, by up to 14.0 points on agent-trace observability; Tez leads on customer service | BENCHMARKS.md §1 (typed-decisions by workflow; results/h2h/summary.json) |
| [`teacher_distribution.png`](teacher_distribution.png) | Scored against the teacher's full distribution, zero-shot Tez's soft accuracy is 0.575 (Jev published 0.580); laya-typed-decisions, trained on it, has the lowest Brier, 0.061 | BENCHMARKS.md §1 (soft accuracy and Brier vs teacher distribution; results/h2h/summary.json); Jev: third-party published |

### Languages

| Chart | Finding (its title) | Source |
|---|---|---|
| [`languages_massive.png`](languages_massive.png) | One frozen decoder reads all 11 MASSIVE languages at 0.790–0.930; Laya's English checkpoint falls to 0.000 (km) and laya-multilingual to 0.210 (km) | BENCHMARKS.md §1 Languages (results/h2h/summary.json) |

### Calibration, robustness and abstention

| Chart | Finding (its title) | Source |
|---|---|---|
| [`calibration.png`](calibration.png) | Tez ships at a mean ECE-15 of 0.140 (0.212 at T = 1, before its default temperature); one temperature per task brings Tez to 0.078 and Laya's checkpoints to 0.068–0.103 | BENCHMARKS.md §1 (Calibration, order robustness, latency), §5c (Tez's default temperature; results/calibration/default_temperature.json, anchor_28); Jev: third-party published |
| [`order_flip.png`](order_flip.png) | Reversing the 20 options of MASSIVE-en changes 0.07 of Tez's answers, against 0.15–0.20 for Laya's checkpoints and 0.13 published for Jev | BENCHMARKS.md §1 (option-order flip; results/h2h/summary.json flip_rate); Jev: third-party published |
| [`conformal.png`](conformal.png) | At 90 % coverage, conformal sets let zero-shot Tez act on 50 % of typed-decisions at 0.853 accuracy and escalate the rest; laya-typed-decisions, fine-tuned on it, acts on 63 % at 0.876 | BENCHMARKS.md §4 (experiments/conformal_td.py, results/conformal_typed_decisions.json) |
| [`batch_calibration.png`](batch_calibration.png) | Batch Calibration (Zhou et al. 2023), fitted out of fold, lowers NLL on 10 of the 11 sets shown, most on prompt-injections (2.89 → 1.03) | BENCHMARKS.md §4 (experiments/batch_calibration.py, results/batch_calibration.json) |

### Speed

| Chart | Finding (its title) | Source |
|---|---|---|
| [`latency_per_task.png`](latency_per_task.png) | One question per call, Laya's checkpoints answer in 24–56 ms; Tez over HTTP, with the question-first layout of §1, took 65–589 ms per decision on these tasks | BENCHMARKS.md §1 (experiments/bench_h2h.py, results/h2h/summary.json). Measured with the question-first layout; BENCHMARKS.md §5b re-measures the runtime's paths |
| [`speed_many_questions.png`](speed_many_questions.png) | 50 questions about one state: 1,357 ms in process with one decode for every question's suffix (27.1 ms each), against 10,511 ms for tez serve as deployed when measured | BENCHMARKS.md §5b (Many questions about one state; results/speed/summary.json, results/speed/tables.md) |

### Voice

| Chart | Finding (its title) | Source |
|---|---|---|
| [`voice_partial_transcripts.png`](voice_partial_transcripts.png) | On the first fifth of a command the top answer is 'none' (0.744 of prefixes) far more often than the gold action (0.222); by the last fifth the gold action leads (0.845): a confident 'none' on a prefix means keep listening | BENCHMARKS.md §3 (streaming; experiments/stream_analysis.py's buckets over results/voicefast_final_last_gemma4-12b-q8_0_stream.jsonl) |
| [`voice_outcomes.png`](voice_outcomes.png) | Voice to action on 220 commands: intent accuracy 0.905 (0.982 accepting the second action of compound commands), and 1 harmful action in 198 actionable utterances with the class-aware commit | BENCHMARKS.md §3 (results/voicefast_final_last_gemma4-12b-q8_0.summary.json, results/stream_policy_final_last_gemma4-12b-q8_0.json) |
| [`voice_budget.png`](voice_budget.png) | About 85 ms from a spoken word to an action: 40 ms for the ASR partial, 30 ms to decide, 15 ms of HTTP and under 1 ms of policy; before --swa-full the decision alone took 205 ms | BENCHMARKS.md §3 (per-word budget; --swa-full). §5b's clean rerun of the same loop measures 33.5 ms compute and 41.6 ms round trip per word (results/speed/summary.json) |
| [`voice_per_word.png`](voice_per_word.png) | Streamed voice in the clean rerun: 41.6 ms per word on the production server, 35.6 ms in process; a 4B probe trained on prefixes takes 19.6 ms, with 3 harmful actions instead of 1 | BENCHMARKS.md §5b (Streamed voice: word to action; results/speed/summary.json) |
| [`asr.png`](asr.png) | faster-whisper base.en returns a partial transcript in 41 ms (p50) at 7.8 % WER, and the intent read from it agrees with the true text on 97.3 %; small.en takes 120 ms for 7.3 % WER | BENCHMARKS.md §3 (ASR; results/asr_bench_*_gemma4-12b-q8_0.summary.json) |

### SemIf's benchmark

| Chart | Finding (its title) | Source |
|---|---|---|
| [`semif_scale.png`](semif_scale.png) | Symbol binding is scale-gated: on SemIf's authored144, Gemma 4 12B Q8 scores 0.943 (SemIf published 0.813 for its 4B) and Llama 3.2 3B Q4 0.358, near chance | BENCHMARKS.md §2 (results/authored144_*.metrics.json); SemIf's published figures from its repository |
| [`semif_calibration_recipes.png`](semif_calibration_recipes.png) | On SemIf, averaging six option orderings plus a temperature cuts NLL from 0.478 to 0.158 at the same accuracy; Zhao's contextual calibration drops accuracy from 0.951 to 0.778 | BENCHMARKS.md §2 (calibration recipes; results/perm_analysis_gemma4-12b-q8_0.json) |
| [`cascade.png`](cascade.png) | A small-to-large cascade is not worth it: escalating when the 4B's two orderings disagree gives 0.854 at 117 ms, against 0.951 at 110 ms for the 12B alone | BENCHMARKS.md §4 (experiments/cascade_analysis.py, results/cascade_authored144.json) |

### Probes

| Chart | Finding (its title) | Source |
|---|---|---|
| [`probes_typed_decisions.png`](probes_typed_decisions.png) | A linear probe on the frozen Qwen3.5-4B reads typed-decisions at 0.794 (layer −12), above the fine-tuned laya-typed-decisions (0.766), from a model whose own letters score 0.489 | BENCHMARKS.md §4b (Hidden-state probe; results/hidden_probe_qwen35-4b.json, results/hidden_probe_server_gemma4-12b-q8_0.json); Jev: third-party published |
| [`probe_depth.png`](probe_depth.png) | The 4B probe plateaus at 0.793 from layer 20 to 28 of 32, well below the top; the 12B peaks at 0.787 at layer 34 of 48, so the bigger backbone buys no better probe | BENCHMARKS.md §4b (layer sweep, probe backbone; results/hidden_probe_sweep_qwen35-4b.json, results/hidden_probe_sweep_gemma4-12b-nf4_std.json) |
| [`probe_data_efficiency.png`](probe_data_efficiency.png) | With 50 labelled rows per question the 4B probe reaches 0.741, with 200 0.788, past laya-typed-decisions' 0.766 after fine-tuning on all 300 | BENCHMARKS.md §4b (data efficiency; results/hidden_probe_sweep_*.json); Jev: third-party published |
| [`early_readout.png`](early_readout.png) | Reading early is cheap latency: the 4B's probe accuracy plateaus from layer 20 to 28, and the model cut to 24 or 20 of its 32 layers prefills 1.30× or 1.54× faster | BENCHMARKS.md §4b (early readout; results/early_exit_qwen35-4b.json, results/hidden_probe_sweep_qwen35-4b.json) |
| [`task_probes.png`](task_probes.png) | One probe per task on the frozen 4B beats laya-typed-decisions on 5 of 6 of Laya's public tasks and reaches 0.860 on Banking77, against Jev's published 0.870 | BENCHMARKS.md §4b (Task probes; results/hidden_probe_tasks_qwen35-4b.json, results/h2h/summary.json); Jev: third-party published |

### Other levers

| Chart | Finding (its title) | Source |
|---|---|---|
| [`banking77_shortlist.png`](banking77_shortlist.png) | On Banking77's 77 intents, a MiniLM shortlist of 20 lets the 12B reach 0.733 in one pass (the chunked tournament: 0.713 in five); Jev's published 0.870 stays ahead | BENCHMARKS.md §4b (Retrieval-narrowed Banking77; results/retrieval_narrow_banking77_minilm.json); Jev: third-party published |
| [`option_elimination.png`](option_elimination.png) | Eliminating options and rescoring the survivors moves accuracy by at most 2.0 points on any task and costs up to a second pass: a negative result | BENCHMARKS.md §4b (Option elimination; results/option_elimination_gemma4-12b-q8_0.json) |

## The probe lab

### Where and what to read

| Chart | Finding (its title) | Source |
|---|---|---|
| [`lab_logit_lens.png`](lab_logit_lens.png) | Read through the logit lens, the 4B's letters score best at layer 29 (0.583), 9.7 points above its top layer; the 12B's letters rise late and keep improving to full depth (0.704) | BENCHMARKS.md §4c (Where to read: the zero-shot logit lens; results/probe_lab_lens_*.json); 12B at full depth: BENCHMARKS.md §1 |
| [`lab_layer_without_labels.png`](lab_layer_without_labels.png) | The layer can be chosen without labels: past its peak, the intrinsic dimension of unlabelled states bottoms out at layers 24–28, inside the probe's accuracy plateau (layers 20–28) | BENCHMARKS.md §4c (intrinsic dimension; results/probe_lab_intrinsic_qwen35-4b.json, results/hidden_probe_sweep_qwen35-4b.json) |
| [`lab_anytime_depth.png`](lab_anytime_depth.png) | An adaptive stop only ties a fixed cut: stopping when the probe is sure reaches 0.789 at 22.0 layers on average; a fixed cut at 22 layers reaches 0.792 | BENCHMARKS.md §4c (Anytime depth; results/probe_lab_anytime_qwen35-4b.json) |
| [`lab_code_size.png`](lab_code_size.png) | A decision needs 64 numbers: one PCA learned from unlabelled states keeps 0.790 of the full 0.793 with 64 of the state's 2,560 numbers | BENCHMARKS.md §4c (How many numbers does a decision need?; results/probe_lab_variants_qwen35-4b.json); Jev: third-party published |

### Probes and labels

| Chart | Finding (its title) | Source |
|---|---|---|
| [`lab_probe_families.png`](lab_probe_families.png) | On layer 26, plain logistic regression (0.793) is as good as any of the 9 probe families tried; nearest centroid scores 0.700 | BENCHMARKS.md §4c (Probe families on layer 26; results/probe_lab_variants_qwen35-4b.json) |
| [`lab_without_labels.png`](lab_without_labels.png) | Without labels the probe only matches its teacher: taught by the 12B's zero-shot letters it scores 0.709 against the teacher's 0.705; gold labels give 0.793 | BENCHMARKS.md §4c (No human labels; Stacking readouts; results/probe_lab_w2s_qwen35-4b.json, results/probe_lab_labelmodel_qwen35-4b.json, results/probe_lab_stack_qwen35-4b.json); Jev: third-party published |
| [`lab_few_labels.png`](lab_few_labels.png) | With few labels, label the most typical rows first and keep the 12B's zero-shot answers as a prior: 50 per question reach 0.766, the level of laya-typed-decisions fine-tuned on all 300 (0.766) | BENCHMARKS.md §4c (Few labels; results/probe_lab_typical_qwen35-4b.json, results/probe_lab_fewshot_qwen35-4b.json, results/probe_lab_prior_qwen35-4b.json) |
| [`lab_cold_start.png`](lab_cold_start.png) | Cold start: learning only from escalations trails the same number of random labels (0.775 against 0.783 at 2,987 labels); with a random audit slice it keeps pace (0.790 against 0.784 at 3,944) | BENCHMARKS.md §4c (Cold start; results/probe_lab_online2_qwen35-4b.json) |

### Guarantees and transfer

| Chart | Finding (its title) | Source |
|---|---|---|
| [`lab_conformal_selection.png`](lab_conformal_selection.png) | A guaranteed error rate: allowed 5 % wrong, the probe acts on 40 % of decisions and is wrong on 5.2 % of them; the 12B's zero-shot letters act on 24 % | BENCHMARKS.md §4c (Conformal selection; results/probe_lab_confsel_qwen35-4b.json) |
| [`lab_unseen_workflows.png`](lab_unseen_workflows.png) | On a workflow it has never seen, one 'is this answer right?' probe trained on the other three scores 0.622 on average, against 0.490 for the 4B's own letters and 0.704 for the 12B's | BENCHMARKS.md §4c (A universal 'is this answer right?' probe; One head for every question; results/probe_lab_universal_qwen35-4b.json, results/probe_truth_qwen35-4b.json, results/probe_lab_workflow_baselines.json) |
| [`lab_many_decisions.png`](lab_many_decisions.png) | Five questions read after one state in one pass cost 2.7× fewer tokens than one prompt per question, for 1.5 points (0.778 against 0.793 at the best layer) | BENCHMARKS.md §4c (Many decisions from one forward pass; results/probe_multiq_qwen35-4b.json); Jev: third-party published |
| [`lab_crosslingual.png`](lab_crosslingual.png) | Trained on English only, a MASSIVE probe scores 0.803 macro over 11 languages, against 0.524 for laya-multilingual and 0.885 for the 12B's zero-shot letters | BENCHMARKS.md §4c (MASSIVE: one probe trained on English only; results/probe_crosslingual_qwen35-4b.json); 12B and laya-multilingual: BENCHMARKS.md §1 (results/h2h/summary.json) |

### Serving it

| Chart | Finding (its title) | Source |
|---|---|---|
| [`lab_pruned_served.png`](lab_pruned_served.png) | Cut the served 4B where the decision is made: with 24 of its 32 blocks its probe scores 0.793 in 58 ms, against 0.776 in 84 ms for the whole model | BENCHMARKS.md §4c (Depth-pruned Qwen3.5-4B GGUFs; Where a decision's latency goes; results/pruned_server_qwen35-4b.json, results/latency_breakdown.json) |
| [`lab_cut_public_suite.png`](lab_cut_public_suite.png) | Cut to 29 blocks, the 4B's zero-shot letters lose 3.7 points on average over Laya's public suite (28 entries), cut to 24 they lose 17.4: cut for probes, validate per task for letters | BENCHMARKS.md §4c (The cut 4B on Laya's public suite; results/h2h_q4b_L24, _L29, _L32/summary.json) |
| [`lab_voice_commit_bound.png`](lab_voice_commit_bound.png) | A collapsing commit bound buys nothing: on final transcripts it matches the default rule; on partial ones it fires earlier (word 3.56 against 3.87) only with 3 harmful actions instead of 1 | BENCHMARKS.md §4c (Streaming voice: a collapsing commit bound; results/voice_ddm_partial_last.json, results/voice_ddm_final_last.json) |
| [`lab_decide_then_bind.png`](lab_decide_then_bind.png) | Decide first, bind later: a probe trained on one option order reads reversed options with no loss up to layer 14, 2.4 points down at layer 18, but 20.5 down at layer 26 | BENCHMARKS.md §4c (Decide, then bind; results/probe_order_qwen35-4b.json) |

